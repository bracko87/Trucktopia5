/**
 * CompanyBalance.tsx
 *
 * Placeholder component to remove the small balance widget from the UI.
 *
 * NOTE:
 * - This file intentionally renders null so the visual object is removed without
 *   breaking imports in other files. If you prefer the file to be deleted
 *   entirely, let me know and I will remove it and update callers.
 */

import React from 'react'

/**
 * CompanyBalance
 *
 * Intentionally returns null to remove the small company balance object from the UI.
 *
 * @returns null
 */
export default function CompanyBalance(): JSX.Element | null {
  return null
}