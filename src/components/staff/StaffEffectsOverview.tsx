/**
 * StaffEffectsOverview.tsx
 *
 * Minimal placeholder component. The previous detailed "Staff Skills & Position Effects"
 * UI was removed entirely as requested to avoid rendering complex objects into JSX.
 */

import React from 'react'

/**
 * StaffEffectsOverview
 *
 * Returns null to remove the Staff Skills & Position Effects section from the UI.
 *
 * @returns null
 */
export default function StaffEffectsOverview(): JSX.Element | null {
  return null
}
